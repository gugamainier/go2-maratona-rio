[build]
builder = "NIXPACKS"
buildCommand = "npm install && cd client/panel && npm install --include=dev && npm run build && cd ../.."

[deploy]
startCommand = "node --experimental-sqlite server/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

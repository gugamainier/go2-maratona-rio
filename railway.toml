[build]
builder = "NIXPACKS"
buildCommand = "npm install && cd client/panel && npm install && npm run build"

[deploy]
startCommand = "node --experimental-sqlite server/index.js"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
